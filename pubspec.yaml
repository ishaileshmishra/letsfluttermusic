name: demo_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  get_it: ^7.2.0
  lottie: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - items.json
    - assets/images/dancer_icon.png
    - assets/anims/music.json
    - assets/anims/equaliser.json
    - assets/anims/dog.json
    - assets/anims/loader.json
    - assets/anims/bounce.json
